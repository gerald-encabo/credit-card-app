import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
//import  { HashRouter as Router } from 'react-router-dom' 
   //alternative use for gh-pages when using react router
ReactDOM.render(
  //<Router>
    <App />
  //</Router>,
  ,document.getElementById('root')
);
